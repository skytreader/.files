#! /usr/bin/bash

# A collection of installs for my preferred Ubuntu customizations.

# Add all PPA's here
sudo add-apt-repository ppa:webupd8team/java

sudo apt-get update

sudo apt-get install -y unity-tweak-tool compizconfig-settings-manager oracle-java8-installer
