# vim: set ft=sh:

export M3_HOME="/opt"
export JAVA_HOME="/usr/lib/jvm/java-8-openjdk-amd64"
alias sls='./node_modules/serverless/bin/serverless.js'

gittheworks(){
    git config user.name "Chad Estioco"
    git config user.email "cestioco@goodgamestudios.com"
}

# Roll out our own virtualenvwrapper commands because Python2 is making life
# harder than it should be.
#
# Reference: https://packaging.python.org/tutorials/installing-packages/#optionally-create-a-virtual-environment

if [ ! -d ~/.virtualenvs ]; then
    mkdir ~/.virtualenvs
fi

lsvirtualenv() {
    ls -td ~/.virtualenvs/*/ | awk -F '/' '{print $(NF-1)}'
}

mkvirtualenv(){
    python3 -m venv ~/.virtualenvs/$1 && workon $1
}

workon(){
    source ~/.virtualenvs/$1/bin/activate
}
